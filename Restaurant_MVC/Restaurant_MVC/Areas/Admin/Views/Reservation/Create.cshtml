@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AuthoringTagHelpers
@inject IDataSharingService DataSharingService;
@using Restaurant_MVC.Models.SharedData;
@model Restaurant_MVC.Common.Model

@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var customerId = DataSharingService.GetSharedData("customerId");

}

<h1>Create</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @* @foreach (var item in Model.GetAllReservations)
        { *@
            <form asp-action="Create">
                <div class="form-group">
                    <input name="CustomerId" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Name</label>
                    <input name="Name" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Email</label>
                    <input name="Email" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Phone</label>
                    <input name="Phone" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Date</label>
                    <input name="Date" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Time</label>
                    <input name="Time" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">NumberOfGuests</label>
                    <input name="NumberOfGuests" class="form-control" />
                </div>
                <div class="form-group">
                    <input name="CreatedDate" value="@DateTime.Now.ToString("MM/dd/yyyy HH:mm:ss")" class="form-control" />
                </div>
                <div class="form-group">
                    <input name="CreatedById" value="@customerId" class="form-control" />
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
        @* } *@

    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

